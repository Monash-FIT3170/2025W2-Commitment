services:
  dev-env:
    image: commitment3170/commitment-meteor:latest
    container_name: 3170-env
    ports:
      - "3000:3000"
    environment:
      - MONGO_URL=mongodb://mongo:27017/commitmentdb
      - ROOT_URL=http://ec2-3-27-5-58.ap-southeast-2.compute.amazonaws.com:3000
    depends_on:
      - mongo
    tty: true
    stdin_open: true
    networks:
      - app-network

  haskell-api:
    image: commitment3170/haskell-api:latest
    container_name: haskell-api
    volumes:
      - ./commitment:/projects/api
    ports:
      - "8081:8081"
    networks:
      - app-network

  mongo:
    image: mongo:6
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - app-network

volumes:
  mongo-data:

networks:
  app-network:
    driver: bridge
